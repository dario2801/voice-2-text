/**
 * Voice-2-Text hero mark — RESERVED SLOT.
 *
 * The earlier abstract placeholder (concentric ridges/arcs) read like a
 * target/bullseye, so it was removed. The page currently shows only the clean
 * center spine.
 *
 * To bring in the real identity: drop the "Creation of Adam" style
 * human-hand ↔ machine-hand engraving (B&W, grain, transparent bg) into
 * /public, render it from this component, then mount it back on the seam in
 * app/page.tsx where the spine comment marks the spot. See
 * docs/PRE-LAUNCH-ASSETS.md.
 *
 * Kept as a component (not deleted) so the integration point stays in one
 * documented place. Renders nothing until the real asset exists.
 */
export function HeroMotif() {
  return null;
}
